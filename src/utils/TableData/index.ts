import {
  columns as RadioColumn,
  data as RadioData,
} from './Radio.data'

import {
  columns as JudgeColumn,
  data as JudgeData,
} from './Judge.data'

import {
  columns as CheckboxColumn,
  data as CheckboxData,
} from './Checkbox.data'

import { columns as StudentColumn } from './Student.data'

export {
  RadioColumn,
  RadioData,
  JudgeColumn,
  JudgeData,
  CheckboxColumn,
  CheckboxData,
  StudentColumn,
}
